<p style="color: green"><%= notice %></p>

<div class="col-lg-8">
  <div class="card shadow">
    <div class="card-body">
      <%= render @estudiante %>
     </div>
   </div>
 <div class= "mt-3">
  <%= link_to "Editar estudiante", edit_estudiante_path(@estudiante), class:"btn btn-warning" %> |
  <%= link_to "Regresar a estudiantes", estudiantes_path, class: "btn btn-outline-primary" %>
<div class= "mt-3">
  <%= button_to "Eliminar estudiante", @estudiante, method: :delete, class: "btn btn-danger" %>

   </div>
 </div>
</div>
